---
- hosts: local
  connection: local
  gather_facts: false
  tasks:
  - name: Launch ansible cloudformation example.
    cloudformation: stack_name="ansible-cloudformation"
                    state=present
                    region=ap-southeast-2
                    disable_rollback=yes
                    template=cloud-formation.json
    args:
      template_parameters:
        KeyName: mbp
    register: stack

  - name: Add host to temporary inventory.
    add_host: name={{ stack.stack_outputs['PublicIP'] }}
              groups=just_created
              ansible_ssh_user=root

- hosts: just_created
  gather_facts: false
  tasks:
  - name: Update the package manager.
    pacman: name=docker state=installed update_cache=yes

